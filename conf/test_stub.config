/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running stub tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run nf-core/oncoanalyser -profile test_stub -stub --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {

    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '1.h'
    ]

}

params {
    config_profile_name        = 'Stub test profile'
    config_profile_description = 'Stub test dataset to check pipeline function'

    // Input data
    input = params.pipelines_testdata_base_path + '/samplesheet/bam_eval.subject_a.wgts.tndna_trna.minimal.stub.csv'

    // Reference data
    genomes {

        'GRCh38_hmf' {
            fasta         = "temp/GRCh38_masked_exclusions_alts_hlas.fasta"
            fai           = "temp/GRCh38_masked_exclusions_alts_hlas.fasta.fai"
            dict          = "temp/GRCh38_masked_exclusions_alts_hlas.fasta.dict"
            img           = "temp/GRCh38_masked_exclusions_alts_hlas.fasta.img"
            bwamem2_index = "temp/bwa-mem2_index/"
            gridss_index  = "temp/gridss_index/"
            star_index    = "temp/star_index/"
        }

    }

    ref_data_hmf_data_path        = "temp/hmf_pipeline_resources/"

    // Analysis config
    mode                     = 'wgts'
    genome                   = 'GRCh38_hmf'
    create_stub_placeholders = true
}
