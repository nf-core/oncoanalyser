name: lilac
description: Type HLA alleles and call somatic structural variants
keywords:
  - hla
  - typing
  - sv
tools:
  - lilac:
      description: Performs HLA typing and calls somatic structural variants
      homepage: https://github.com/hartwigmedical/hmftools/tree/master/lilac
      documentation: https://github.com/hartwigmedical/hmftools/tree/master/lilac
      licence: ["GPL v3"]
input:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. [id: 'sample_id', tumor_id: 'tumor_name', normal_id: 'normal_name']
  - normal_wgs_bam:
      type: file
      description: Normal WGS BAM file
      pattern: "*.{bam}"
  - normal_wgs_bai:
      type: file
      description: Normal WGS BAI file
      pattern: "*.{bai}"
  - tumor_wgs_bam:
      type: file
      description: Normal WGS BAM file (optional)
      pattern: "*.{bam}"
  - tumor_wgs_bai:
      type: file
      description: Tumor WGS BAI file (optional)
      pattern: "*.{bai}"
  - tumor_wts_bam:
      type: file
      description: Normal WTS BAM file (optional)
      pattern: "*.{bam}"
  - tumor_wts_bai:
      type: file
      description: Tumor WTS BAI file (optional)
      pattern: "*.{bai}"
  - purple_dir:
      type: directory
      description: PURPLE output directory (optional)
  - genome_fasta:
      type: file
      description: Reference genome assembly fa file
      pattern: "*.{fa,fasta}"
  - genome_ver:
      type: string
      description: Reference genome version
  - lilac_resources:
      type: directory
      description: LILAC resources directory
output:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. [id: 'sample_id', tumor_id: 'tumor_name', normal_id: 'normal_name']
  - lilac_dir:
      type: file
      description: LILAC output directory
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
authors:
  - "@scwatts"
